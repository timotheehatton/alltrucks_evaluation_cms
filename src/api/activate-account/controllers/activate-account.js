'use strict';

/**
 * activate-account controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::activate-account.activate-account');
